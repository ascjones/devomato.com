---
layout: default.hbs
---

<div class="content">
	{{#each posts}}
		{{#forEach pages}}
			<h1 class="page-header"><a href="{{relative ../../page.dest dirname}}">{{data.title}}</a></h1>
			{{#if isFirst}}
				{{#markdown}}{{{ page }}}{{/markdown}}
			{{else}}
				{{#markdown}}{{#excerpt}}{{{ page }}}{{/excerpt}}{{/markdown}}
				<p><a href="{{relative ../../../page.dest post.dirname}}">Read the rest...</a></p>
			{{/if}}
			{{> postfooter.hbs data }}
		{{/forEach}}
	{{/each}}
</div>